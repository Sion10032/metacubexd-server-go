# Build artifacts + test data — don't ship into the Docker context.
*.test
*.out
*.coverprofile
profile.cov
coverage.*

# Local Go module cache (not needed in Docker)
.go/

# Nix / direnv
result
.direnv

# Editor
.vscode/
.idea/

# Git (Docker daemon doesn't need this)
.git/
.gitignore

# Project-specific
/data
/internal/static/web
